<h1>GeekTwitter</h1>
<h3>Tweet詳細</h3>
<div class="tweet">
  
  <% @user.tweets.each do |t| %>
        <%= t.user.name %>
        <%= t.body %>
  <% end %>
    
</div>

<%= link_to "Tweet一覧に戻る", tweets_path %>